[project]
name = "novel-analysis-workspace"
version = "0.1.0"
description = "Deterministic local scaffolding for layered long-form novel analysis"
requires-python = ">=3.10"
dependencies = ["jsonschema>=4.18"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
