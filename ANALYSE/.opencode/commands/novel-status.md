---
description: Validate and summarize progress for one imported novel.
agent: novel-orchestrator
---

Load the `novel-analysis` skill. Inspect `$ARGUMENTS` without advancing analysis.

Run source and fact validation, inspect run manifests and output coverage, then report completed ranges, pending jobs, validation failures, unresolved critical entities or threads, and the next safe action.
